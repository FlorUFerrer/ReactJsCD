/*NOTAS DE BOTON*/

/*
const [counter,setCounter] = useState(0)
const decreace = () => setCounter(counter-1)
//const [lastClickDate,setLastClickDate] = useState(new Date());

//  const handleMensaje =() => setMensaje('Soy un mensaje')
const add = () => {
  setCounter(counter+1);
  setLastClickDate (new Date());
}
*/

//<button onClick ={add}>+</button>
  //       <Mensaje text ={counter}/>
    //     {/* <Mensaje text={mensaje} onMensaje= {handleMensaje}/> */}
      //    <button onClick ={decreace}>-</button> 
        //  <h4>Ultimo click: {lastClickDate.toString()}</h4> 
